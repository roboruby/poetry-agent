# Changelog

## Unreleased

- Initial extraction: the `poetry-agent` MCP server moves here from poetry-core
  (one gem, one executable - the core/ui exe parity doctrine retires).
- The WebMCP runtime: declared component tools registered with
  `document.modelContext` per opted-in instance; declarative forms; the
  origin-trial middleware.
